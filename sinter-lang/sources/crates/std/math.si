fn sqrt(x: u64, y: u64) => f64 {
    0.0
}

fn max(x: u64, y: u64) => u64 {
    if x > y {
        x
    } else {
        y
    }
}

fn min(x: u64, y: u64) => u64 {
    if x < y {
        x
    } else {
        y
    }
}