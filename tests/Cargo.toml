[package]
name = "tests"
version = "0.1.0"
edition = "2021"

[dev-dependencies]
compiler = { path = "../compiler" }
diagnostics = { path = "../diagnostics" }

ron = "0.8.1"
serde = "1.0.197"

[dependencies]
pretty_assertions = "1.4.0"
